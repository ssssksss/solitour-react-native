import React from 'react';
import SvgLogo from '../../assets/img/logo/solitour-logo.svg'; // SVG를 컴포넌트처럼 가져오기

const LogoTitle = () => {
  return <SvgLogo width={50} height={50} />;
};

export default LogoTitle;
